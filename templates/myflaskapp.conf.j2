#	{{ ansible_managed }}

server {
    listen 80;
    server_name {{ flaskapp_server_name }};

    location / {
        include uwsgi_params;
        uwsgi_pass unix:{{ flaskapp_root_dir }}/myflaskapp.sock;
    }
}